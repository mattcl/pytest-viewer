import { outcomeStyle } from '../../../helpers/outcome-style';
import { module, test } from 'qunit';

module('Unit | Helper | outcome style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = outcomeStyle(42);
  assert.ok(result);
});
