import levelForOutcome from '../../../utils/level-for-outcome';
import { module, test } from 'qunit';

module('Unit | Utility | level for outcome');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = levelForOutcome();
  assert.ok(result);
});
